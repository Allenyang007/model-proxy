{
  "name": "model-proxy",
  "version": "1.0.0",
  "description": "OpenAI-compatible model proxy with fallback, retry, and parallel request support",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
